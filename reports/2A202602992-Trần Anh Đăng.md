# Individual contribution report

## Thông tin

- Họ và tên: Trần Anh Đăng
- Mã học viên: 2A202602992
- Nhóm:Sói cô độc
- Repository/branch: `K4-L3B-RAG-Pipeline` / `main`

## Phần việc đã thực hiện

| Module/deliverable                       | Việc tôi trực tiếp làm                                                                                                                                  | File/commit/PR                                     | Trạng thái |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ---------- |
| Task 4 - Chunking, embedding và indexing | Cài đặt đọc Markdown từ `data/standardized`, chia chunk bằng `RecursiveCharacterTextSplitter`, embed chunks và upsert vào ChromaDB với cosine distance. | `src/task4_chunking_indexing.py`, commit `ff2f113` | Done       |
| Task 5 - Semantic search                 | Cài đặt dense retrieval: embed query bằng cùng hàm `embed_texts`, query ChromaDB, đổi cosine distance thành similarity score và trả `SearchResult`.     | `src/task5_semantic_search.py`, commit `ff2f113`   | Done       |
| Task 6 - BM25 lexical search             | Cài đặt tokenize, build BM25 index trên corpus chunks, trả kết quả theo contract; bổ sung fallback keyword overlap cho corpus nhỏ/test case.            | `src/task6_lexical_search.py`, commit `ff2f113`    | Done       |
| Task 7 - Reciprocal Rank Fusion          | Cài đặt RRF để gộp dense results và BM25 results, deduplicate theo chunk ID, set `retrieval_method="hybrid"`.                                           | `src/task7_reranking.py`, commit `ff2f113`         | Done       |
| Repo hygiene                             | Thêm `chroma_db/` vào `.gitignore` để không commit vector database/cache lên GitHub.                                                                    | `.gitignore`, commit `ff2f113`                     | Done       |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Dùng một hàm `embed_texts()` chung cho cả indexing và semantic search.  
   **Lý do/evidence:** Contract yêu cầu Task 4 và Task 5 dùng chung embedding model/dimension; test `semantic_search` kiểm tra query dùng đúng shared embedding.  
   **Trade-off:** Khi đổi embedding model cần index lại ChromaDB để tránh lệch dimension.

2. **Quyết định:** Dùng RRF để gộp dense search và BM25 thay vì cộng trực tiếp score.  
   **Lý do/evidence:** Cosine similarity và BM25 score khác thang đo; RRF chỉ dựa trên thứ hạng nên ổn định hơn. Test `rrf` đã pass.  
   **Trade-off:** RRF score không phản ánh confidence tuyệt đối, nên không dùng score này để quyết định fallback.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng:
  - `python -m src.task4_chunking_indexing`
  - `python -m src.task5_semantic_search`
  - `python -c "from src.task6_lexical_search import lexical_search; import json; print(json.dumps(lexical_search('Luật Du lịch hướng dẫn viên du lịch', 3), ensure_ascii=False, indent=2))"`
  - `pytest tests/test_contracts.py -q -k "chunk_documents or semantic_search or lexical_search or rrf"`
- Kết quả trước/sau nếu có:
  - Task 4 index thành công `1271 chunks`.
  - Task 5 trả kết quả dense search có đủ `id`, `content`, `score`, `metadata`, `retrieval_method`.
  - Task 6 trả kết quả BM25 đúng chủ đề hướng dẫn viên du lịch.
  - Contract test riêng Task 4-7: `4 passed, 11 deselected`.
- Lỗi đã phát hiện và cách xử lý:
  - Model `BAAI/bge-m3` tải rất lớn và chạy chậm trên máy local, nên cấu hình sang `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` để chạy thực tế nhanh hơn.
  - Metadata từ ChromaDB có thể thiếu `url` khi giá trị ban đầu là `None`, nên bổ sung normalize metadata trước khi trả search results.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Dense retrieval với model nhẹ có thể chưa chính xác bằng embedding model lớn hoặc OpenAI embedding, đặc biệt khi dữ liệu crawl còn lẫn nội dung sidebar/tin liên quan.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Làm sạch corpus tốt hơn và so sánh A/B giữa local embedding, OpenAI embedding và BM25/hybrid để chọn cấu hình retrieval tốt nhất.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Trần Anh Đăng
